#include <stdio.h>

void main(){
	char text[1000];
	int i = 0;
	printf("enter your text \n ");
	fflush(stdout);
	gets(text);
	for(  i = 0;text[i]!='\0';i++){

	}
	 printf("length of the text is : %d",i);

}

